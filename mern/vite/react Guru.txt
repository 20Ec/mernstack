npm create vite@latest my-react-app 
react 
js 


✔ Project name: my-react-app
✔ Select a framework: › React
✔ Select a variant: › JavaScript

npm  run dev 
 
npx create-react-app  my-react-app

id,class 


add index file in css and js using link tag bootstrap 
 
css:  
    <link
  href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
  rel="stylesheet"
/>


js:
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

add external css :

import './App.css'


index.html  →  main.jsx  →  App.jsx  →  YourComponent.jsx


incase use id you want to add !important at last only its works !important

frist create component folder and create  components 
you want to add component in app.jsx 
 <Header />



 Routes


npm install react-router-dom


src/pages 
home.jsx
about.jsx
contact.jsx



add app.jsx


import { BrowserRouter as Router, Routes, Route, Link } from 'react-router-dom'
import Home from './pages/Home.jsx'
import About from './pages/About.jsx'
import Contact from './pages/Contact.jsx'


BrowserRouter → Wraps your app to enable routing.

Routes → Container for all your routes.

Route → Each route has:

path → URL path

element → Component to render

Link → Navigation links (like <a> but doesn’t reload the page).

 to set the address then to path to match to then and element for bus = components 


props 
this is using similar to function 
const Header = (props) => {
 in side the header use props first 
then add inside the content  {props.name}

then call in app.jsx file link this 

<Header title="Welcome to My Site" subtitle="Learn React with Vite" />


function Header({ title, subtitle }) {
  return (
    <div>
      <h1>{title}</h1>
      <p>{subtitle}</p>
    </div>
  );
}



function Header({ title = "Default Title", subtitle = "Default Subtitle" }) {
  return (
    <div>
      <h1>{title}</h1>
      <p>{subtitle}</p>
    </div>
  );
}


props with default valiues

function Header({ title = "Default Title", subtitle = "Default Subtitle" }) {
  return (
    <div>
      <h1>{title}</h1>
      <p>{subtitle}</p>
    </div>
  );
}


 use state 

import { useState } from 'react';


2️⃣ Forms & Controlled Components

Learn how to handle user input:

<input>, <textarea>, <select>

Controlled vs uncontrolled components

Example: TodoList with input field to add custom tasks

Form validation (simple checks, required fields)

3️⃣ Advanced Hooks

useEffect for API calls / side effects

useReducer for complex state logic

useContext for global state management

useMemo / useCallback for performance optimization

Goal: Build dynamic apps with efficient state management.





Week	Focus
1	Fundamentals, Components, State, Routing
2	Styling: CSS Modules / Bootstrap / Tailwind
3	Forms & Controlled Inputs
4	API Integration & useEffect



